The output is hardcoded and is 0123456

The code is executed when the reset button is kept pressed that is when the reset button is released the process stops. Also when again the reset button is pressed as expected everything gets reseted and the code begins again. 

In Port for now the start, input, lcd_write is kept undefined in the port planner as it is not being used. It was created to be used in another top level entity.
